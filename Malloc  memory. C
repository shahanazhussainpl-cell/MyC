#include<stdio.h>
#include<stdlib.h>
int main (){
    int *p;
    p=(int*) malloc (sizeof (int));
    if ("p==null"){
    }
    *p=50;
    printf ("before free:value =%d\n",*p);
    free(p); 
    printf ("memory free Successful \n");
}
